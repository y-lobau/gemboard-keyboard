# Test Cases

| ID    | Scenario                                                                                                                                                                                                                                                                         | Coverage                              |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- |
| TC-01 | The host app main tab renders Gemini API-key setup first, then the onboarding summary, then the draft composer with the enlarged draft text area, without in-app recording controls.                                                                                            | `__tests__/App.test.tsx`              |
| TC-02 | Saving a Gemini API key persists through the native config bridge and updates the status message.                                                                                                                                                                                | `__tests__/App.test.tsx`              |
| TC-03 | On iOS, keyboard-extension mode renders enablement guidance for adding Plyń in iPhone keyboard settings and shows the companion session state.                                                                                                                                   | `__tests__/App.test.tsx`              |
| TC-06 | On iOS, when a saved API key is present, the host app automatically starts the keyboard companion session and updates its status after the native session bridge responds.                                                                                                       | `__tests__/App.test.tsx`              |
| TC-07 | On iOS, opening the app through the `plyn://session` deep link immediately retries the companion session.                                                                                                                                                                        | `__tests__/App.test.tsx`              |
| TC-08 | On iOS, the keyboard extension main layout shows only the erase control, microphone control, and waveform indicator during the ready state.                                                                                                                                      | Manual simulator validation           |
| TC-09 | On iOS, the keyboard extension microphone starts capture on touch-down and stops capture on touch-up instead of using tap-to-toggle.                                                                                                                                             | Manual simulator validation           |
| TC-10 | On iOS, while the keyboard is recording or waiting for the companion app to process dictation, the erase, space, enter, and microphone controls become inactive until the keyboard returns to an interactive state.                                                           | Manual simulator validation           |
| TC-11 | On iOS, if the companion app does not respond in time, the keyboard extension shows a clear error message.                                                                                                                                                                       | Manual simulator validation           |
| TC-12 | On iOS, if the companion app does not respond in time, pressing the keyboard microphone opens the Companion app into the session recovery flow.                                                                                                                                  | Manual simulator validation           |
| TC-13 | On app startup, the host app triggers Firebase Remote Config sync for the Gemini runtime configuration.                                                                                                                                                                          | `__tests__/App.test.tsx`              |
| TC-14 | Remote Config initialization persists the Gemini model, system prompt, and keyboard timeout values into the native config bridge, returns transcription cost rates for the host-app UI on both iOS and Android, preserves previously stored runtime values when a refresh fails, and seeds bundled fallback runtime values when Firebase is empty or unavailable. | `config/remoteConfigService.test.ts`  |
| TC-15 | Starting a new keyboard dictation session triggers a native Remote Config refresh before Android builds the Gemini request so the current transcription can use updated runtime values without a full app relaunch.                                                            | Manual device validation              |
| TC-16 | The host app records analytics screen views for the main and settings tabs without logging user text content.                                                                                                                                                                    | `__tests__/App.test.tsx`              |
| TC-17 | Saving a Gemini API key records analytics for the attempt and the result without logging the key value.                                                                                                                                                                          | `__tests__/App.test.tsx`              |
| TC-19 | Remote Config sync records analytics for success and failure outcomes, and if Firebase Remote Config cannot be accessed it also records a non-fatal Crashlytics error without crashing the app.                                                                              | `config/remoteConfigService.test.ts`  |
| TC-20 | On iOS, companion session auto-start, manual toggles, and deep-link retries record analytics with their triggering source and result.                                                                                                                                            | `__tests__/App.test.tsx`              |
| TC-21 | The host app keeps platform keyboard-help buttons and the iOS companion-session controls inside a collapsed onboarding details section until the user expands it.                                                                                                                | `__tests__/App.test.tsx`              |
| TC-22 | On iOS, if saving the Gemini API key succeeds but restarting the companion session fails, the app still confirms the key was saved and separately reports that the companion session is inactive.                                                                                | `__tests__/App.test.tsx`              |
| TC-23 | On app startup, the host app initializes Firebase Crashlytics for the current platform.                                                                                                                                                                                          | `__tests__/App.test.tsx`              |
| TC-24 | The Crashlytics service enables collection, tags the current platform, and can trigger a test crash for debug verification.                                                                                                                                                      | `services/crashlyticsService.test.ts` |
| TC-25 | Opening `plyn://debug/launch` shows an in-app preview of the loading view instead of the normal main screen.                                                                                                                                                                     | `__tests__/App.test.tsx`              |
| TC-26 | Once the Gemini API key is already saved, the first setup section collapses to a tappable `Set up` header and expands its controls only after the user opens it.                                                                                                                 | `__tests__/App.test.tsx`              |
| TC-27 | On Android, after the user releases the microphone, streamed Gemini transcript snapshots update the active editor progressively without duplicating previously inserted text or collapsing required spaces between words.                                                      | Manual device validation              |
| TC-28 | On Android, if a dictation session becomes stale or the active editor changes before streaming finishes, later transcript snapshots are ignored instead of inserting into the wrong field.                                                                                       | Manual device validation              |
| TC-29 | On iOS, after the user releases the keyboard microphone, the companion session publishes progressive transcript snapshots and the extension replaces only its own provisional insertion for the active utterance without collapsing required spaces between words.              | Manual simulator validation           |
| TC-30 | On iOS, if the extension can no longer safely replace its provisional insertion, it stops applying later transcript snapshots instead of duplicating or corrupting surrounding text.                                                                                             | Manual simulator validation           |
| TC-31 | On both platforms, if Gemini fails before any transcript snapshot is inserted, the existing text remains unchanged and the keyboard returns an error or retry-ready state.                                                                                                       | Manual device validation              |
| TC-32 | On both platforms, if Gemini fails after partial transcript snapshots were already inserted, the latest inserted snapshot remains in place without duplicate text.                                                                                                               | Manual device validation              |
| TC-33 | On iOS, if the config and session bridges become available after the first React render, the host app refreshes from the native bridges and shows the saved API-key and companion-session state instead of staying on fallback values.                                           | `__tests__/App.test.tsx`              |
| TC-36 | The bottom `Кошт транскрыпцыі` summary stays collapsed by default and expands into `Апошні запыт`, `Усяго`, and `Сярэдняе на запыт` groups in that order, each showing `IN` and `OUT` rows as `tokens / dollars`, with the value column labeled `токены/$`. | `__tests__/App.test.tsx`              |
| TC-37 | The host app restores the saved expanded or collapsed state for the `Як гэта працуе`, `Наладзьце Plyń`, and `Кошт транскрыпцыі` sections after a relaunch.                                                                                                                     | `__tests__/App.test.tsx`              |
| TC-39 | On iOS, if the companion background session is interrupted or suspended by another app audio session, reopening the companion app retries the session automatically so the keyboard can recover without a full app relaunch.                                                   | `ios/PlynKeyboardE2ETests/PlynSessionRecoveryStateTests.swift` |
| TC-40 | The expanded transcription-cost summary provides a compact reset action that clears both the latest successful request stats and the cumulative token totals on the current device.                                                                                              | `__tests__/App.test.tsx`              |
| TC-41 | On iOS, shortly returning to the keyboard after reopening the companion app still treats the companion session as alive during the recovery handoff window instead of immediately re-entering the `open companion app` loop.                                                  | `ios/PlynKeyboardE2ETests/PlynCompanionSessionLivenessTests.swift` |
| TC-42 | On iOS, streamed transcript assembly preserves boundary whitespace when Gemini emits a required separator at the end of one chunk or in its own chunk, so adjacent words do not merge.                                                                                      | `ios/PlynKeyboardE2ETests/PlynSharedStoreTranscriptMergeTests.swift` |
| TC-43 | On iOS, pressing erase at the start of an empty line removes only the immediately preceding newline instead of also deleting the previous word.                                                                                                                               | Manual simulator validation           |
